# =====================
# Configurações do banco de dados MySQL
# =====================

spring.datasource.url=jdbc:mysql://localhost:3306/lavafacil
spring.datasource.username=root
spring.datasource.password=12345
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# =====================
# JPA / Hibernate
# =====================
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.database-platform=org.hibernate.dialect.MySQL8Dialect

# =====================
# Configurações de envio de e-mail (Gmail SMTP)
# =====================
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=silvajasiel30@gmail.com
spring.mail.password=ujvqktliehzumkuv
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true
spring.mail.properties.mail.transport.protocol=smtp
spring.mail.default-encoding=UTF-8

# =====================
# Google Service Account
# =====================
google.service.account.key=classpath:credenciais.json

# =====================
# Porta do servidor (usada no Render)
# =====================
server.port=${PORT:8080}
